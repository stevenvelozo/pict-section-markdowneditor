{
	"name": "embedded_editor_example",
	"version": "1.0.0",
	"description": "Small embedded markdown editor with bordered container",
	"main": "EmbeddedEditor-Example-Application.js",
	"scripts": {
		"start": "node EmbeddedEditor-Example-Application.js",
		"build": "npx quack build && npx quack copy"
	},
	"author": "steven",
	"license": "MIT",
	"copyFilesSettings": {
		"whenFileExists": "overwrite"
	},
	"copyFiles": [
		{
			"from": "./html/*",
			"to": "./dist/"
		},
		{
			"from": "../../node_modules/pict/dist/*",
			"to": "./dist/"
		}
	],
	"dependencies": {
		"pict": "^1.0.350",
		"pict-application": "^1.0.19",
		"pict-view": "^1.0.66"
	},
	"devDependencies": {
		"@codemirror/lang-markdown": "^6.5.0",
		"@codemirror/state": "^6.5.4",
		"codemirror": "^6.0.2",
		"esbuild": "^0.27.3"
	},
	"retold": {
		"ExampleApplication": {
			"Stage": true,
			"Title": "Embedded Editor",
			"Summary": "The smallest useful integration — pict-section-markdowneditor mounted inside a bordered card on an ordinary page, ready to drop into a notes field or comment box.",
			"Complexity": "Basic"
		}
	}
}